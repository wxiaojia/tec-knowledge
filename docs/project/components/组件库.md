# 组件（库）开发的设计与开发思路（细节）

重难点: 1.组件库、通用库架构设计 2.组件库方案选型与细节设计与实现 3.组件库构建发布与持续迭代

# 【初中级】开发过组件库吗，请说说你的设计与开发思路(要细节)?

### 架构设计

- 分层
  - rc-xXX，提供基础组件，unstyled component(headless)，只具备功能交互不具备 UI 表现
  - 样式体系，theming
  - 基础组件
  - 复合组件，Search,Input +Select,IconButton Icon + Button
  - 业务组件
  -
- 解耦
  - 对于每个组件都需要定义样式、ts 类型、基础操作、工具方法响应式设计
  - 媒体查询 media query、Resize0bserver、Grid

### 状态管理

- 全局状态，基础配置、国际化配置、主题配置，react→>Context、useSyncExternalStore，vue ->vue-demi
- 局部状态，表单场景，受控和非受控(状态是否跟表单值双向奔赴)
  ```js
  inputvalue={v} onchange={()= setV}(受控的)
  ```

### 样式体系与主题设计

- Color·Tokens:颜色色值系统，(antd、arcomantineUI·mui.shadcn/ui)
- 样式模块化方案:css-in-js(弊端:运行时、ss 支持)【emotion、styled-components】，module css
- 样式优先级与覆盖:控制样式优先级

### 模块化

- 可复用性，对于 props、events 的设计非常重要，为什么 input、textarea 都需要 value onChange 成对(规则：value 作为值，onChange 作为值的响应方法)
- 公共方法:颜色计算函数，格式化处理，本地化，工具函数- @ant-design/utils

### 开发流程

#### 本地开发

二次开发，antd 来做二次开发

### 组件库开发流程

1. 工程架构:monorepo,core + components + hooks + utils + shared
2. Typescript
3. 流程化、规范化、自动化
   1. script 如何定义（CI、CD 的源头） - 流程化
   2. eslint9\stylelint\spelllink\commitlink - 规范化
   3. 颜色值生成，自动构建，增量构建，构建缓存 - 自动化
4. 构建打包:rollup、esbuild、swc
5. 测试，单元测试，vitest，jest+react-tesing-library

```js
  "peerDependencies": {
    "ant-design-vue": ">=4.2.5",
    "vue": ">=3.4.0"
  }
```

peerDependencies 基础依赖，前置依赖（一般是第三方的内设置的依赖）

#### tsconfig 中：

```js
{
  "compileroptions":{
    "module": "esnext",
    "target": "ES6",
    "jsx": "react",
    "declaration": true,  // 生成声明文件
    "declarationDir":"./es/types" // 生成声明文件目录
    "outDir":"./es" // 输出目录
    "moduleResolution":"node" // 模块解析器
    "strict": true,
    "allowSyntheticDefaultImports":true,  // 允许使用默认导出
  } 
  "include":["src/**/*"]
  }
```


# 【中高级】请从零到一实现一个组件库基础框架
## 技术选型与方案​
​
1.包管理工具：pnpm workspace - 实现多包管理，提升安装速度并减少磁盘空间占用。​

2.组件库开发语言：TypeScript - 提供类型安全，增强代码健壮性，并支持静态类型检查。​

3.文档与组件展示：dumi - 构建文档站点，支持组件展示、API 文档生成以及实时预览。​

4.构建工具：TypeScript 编译器 - 使用 tsc 进行代码编译，生成 ES 模块和 CommonJS 模块，保证兼容性。​

5.样式管理：CSS-in-JS（可选） - 可选使用 CSS-in-JS（如 Emotion、styled-components）实现动态样式和主题化。​

6.组件组织：模块化与分层设计 - 按模块化组织，基础组件与复合组件分离，确保复用性和解耦性。​

7.测试框架：Jest + Testing Library（扩展） - 使用 Jest 和 Testing Library 进行单元测试和集成测试，确保组件库功能正确性。​

8.版本控制与 CI/CD：GitHub Actions（扩展） - 配置 GitHub Actions 实现自动化测试和发布流程，保证代码提交后的质量。​

9.构建产物发布：NPM - 使用 pnpm publish 发布构建产物，便于组件库的版本管理和分发。

优化：更合适的方案，例如:
- 构建:esbuild
- 整体架构:pnpm workspace 改良
- 构建环节:nx，构建缓存和增量构建
- CI/CD 流水线编排
- 文档发布 docker-compose 服务编排部署


步骤：
### 初始化工程
### 依赖选择
### ts 配置，构建配置
### monorepo 分包
### 组件库文档编写
### 构建发布：

#### 组件库发布
- pnpm build
- pnpm publish
#### 组件文档的发布
选择docker nginx, 静态资源托管来处理 

```yml
# docker-compose.yml
services:
  miaoma-ui-docs
    image: nginx:lastest
    container_name:miaoma-ui-docs
    port:
      - 8080:8080
      - 8090:8090
    volumn:
      - ./:/app
```
启动服务:
```bash
docker compose -f docker-compose.yml up -d
```

### 版本管理
`np`

版本号的规则:1.0.0，这个版本号代表什么? 主.次.修复

x.x.x-beta 测试发行版

# 【专家级】如果让你架构设计一个类似 Ant Design 组件库，如何设计并落地？​
## Ant Design 组件库的源码目录结构概览​
​
在设计组件库之前，先了解 Ant Design 的目录结构和各模块的功能是很有帮助的。​
Ant Design 5 的源码目录结构大致分为以下几个主要部分：​
​
- components：包含核心组件。每个文件夹是一个独立的组件，包含 index.tsx（组件实现）、style.ts（样式）、test.tsx（测试）等。​
- rc-components：基础组件库，提供逻辑独立、无样式的基础功能组件，例如 rc-tree、rc-tabs 等。​(模块化和复用)
- styles：Ant Design 的样式解决方案 antd-style，实现 cssinjs，支持主题定制与动态切换。​
- tools：构建和发布工具 antd-tools，用于打包、构建、发布和测试自动化。​
- scripts：构建和发布相关的脚本，例如自动化打包、文档生成、CI/CD 脚本。​
- docs：文档文件夹，主要用来管理组件文档、演示代码和网站配置。​
- tests：测试文件夹，包含单元测试和集成测试。