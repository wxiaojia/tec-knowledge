

## 组件复杂度
“一个组件的复杂度，主要来源就是自身的状态；即组件自身需要维护多少个不依赖于外部输入的状态。”

## 顺序：
1、确定组件的功能；
2、确定组件接收的数据；
3、确定组件自身维护的状态；
4、确定组件的操作功能；
5、确定组件输出的数据；


## 封装技巧
- 与ant-desgin-vue相同的参数，不需要卸载props中，可以结合props和attrs获取元素属性，避免冗余代码

- 属性解构


