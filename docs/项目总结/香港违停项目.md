
### [香港违停项目] 前端总结
#### 一、项目概况  
- 周期：2025/03/24 ~ 2025/04/24
- 团队：前端1人、后端1人
- 核心功能：首页展示、系统设置、事件配置修改、其他小更改

- 技术实现总结
    使用空客SSS的项目框架，用vue3+vite3+naiveui; 也有gendo-ui-vue3; （但为了项目样式统一，大部分还是使用了naiveui）；
    后续更新了一些依赖版本，主要是vite3--->vite5
    - vite升级其实搬运了玄同的vite4,下载的5的版本，然后再运行，如果报错就修改错误
    - jenkins构建的时候，因为升级了vite，也升级了node21.x，在打包上有问题，本地打包没问题，求助了强哥
    -- 解决：修改build位置，改成在docker中构建，【学习点】


docker compose down 删除容器
docker compose up -d 再重启

#### 二、关键成果  


#### 三、问题/挑战/学习  
学习点：
1、jenkinsfile修改为在docker中构建；
2、项目使用了翻译，il8n，但有点麻烦，其实修改了一处文字，对应的语言也要修改；
3、项目在arm中构建运行，ws-tcp-relay，需要切成arm支持的版本，跟x86的不兼容，在arm中构建的话，docker命令一样的；也有其他方式，docker buildx。。。没有试过
4、遇到echarts图的坑，使用shallRef

难点：
1、事件配置中绑定摄像头的配置逻辑和画区域；
2、事件配置中的编辑事件轴逻辑；目前还有个偶现项目没有解决

疑问：
1、jenkinsfile怎么指定部署服务器？使用agent节点？但节点又怎么看服务器？
根据agent节点，指定服务器，看节点日志可以看到，使用sshLaucher链接，
（ssHlauncherfhost='192,168.20,212', port=22, credentialsId='af597f69-07d8-4519-93d1-4ee5bb28ee0d', ivmootions='.
iavaPath='/opt/idk-21.0,1/bin/iava', prefixstartslavecmd=', suffixstartslavecmd='
launchrineoutserconds=60, mexumpetries=10,retryweaitTine-15,shHastkeherificationstrategyethadsom.plugins.shslaves.verifiers.onerifyingkeherificationstratey tcpaelaytrue, tracdkcredentials-true）
一些配置，只看懂第一行内容；
指定服务器后，可以docker-compose.yaml中，配置容器内外端口。
（需不需要在服务器中设置什么？）
（直接跑项目中的docker-compose的吗，不需要在其他地方设置了？直接docker-compose中动态的设置了镜像和版本）


#### 四、改进方案  
| 问题点         | 解决方案                          | 责任人 | 时间节点 |  
|----------------|-----------------------------------|--------|----------|  

#### 五、个人成长  
- 更多了解jenkins\arm
- 想去更多了解docker内容，以及kubernetes k8s内容：https://www.qikqiak.com/k8s-book/docs/2.Docker%20%E7%AE%80%E4%BB%8B.html



